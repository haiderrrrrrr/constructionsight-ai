import TabProjectDetails from '../projectsCreate/TabProjectDetails';

const TabPMDetails = ({ formData, setFormData, errors, setErrors }) => {
  return <TabProjectDetails formData={formData} setFormData={setFormData} errors={errors} setErrors={setErrors} />;
};

export default TabPMDetails;
